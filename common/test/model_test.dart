import 'package:test/test.dart';
import 'package:common/common.dart';

void main() {
  group('Guessing Pokemon', () {
    test('should guess pokemon', () {
      String actualValue = "";
      String expectedValue = "";
      expect(actualValue, equals(expectedValue));
    });
  });
}
